import Button from '@material-ui/core/Button';

function Register({ variant, color, size }) {
  return (
    <Button variant={variant} color={color} size={size}>
      Register
    </Button>
  );
}

export default Register;
